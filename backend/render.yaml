services:
  - type: web
    name: opsagent-api
    env: python
    buildCommand: pip install fastapi uvicorn python-dotenv httpx groq
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: GROQ_API_KEY
        sync: false
